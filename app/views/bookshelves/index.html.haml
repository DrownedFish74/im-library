= render "layouts/header"
.all-bookshelves
  -if @user.id == current_user.id
    = render "bookshelves/form"
    = render "bookshelves/wish-form"
    = render "my-bookshelves" 
  -else
    = render "bookshelves/wish-form"
    = render "other-bookshelf"
